<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.app.activity"
    android:revisionCode="0"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk android:minSdkVersion="10" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logotipo"
        android:label="   Gestão de Equipes"
        android:supportsRtl="true"
        android:theme="@style/AppTema">

        <!-- Activity Main do App -->
        <activity
            android:name="br.com.app.MainActivity"
            android:label="   Gestão de Equipes"
            android:logo="@drawable/ic_logotipo"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
